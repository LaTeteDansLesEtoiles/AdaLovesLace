<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.alienlabs.adaloveslace</groupId>
  <artifactId>adaloveslace</artifactId>
  <version>1.0.17</version>
  <description>A thread-based techniques diagrams creation software</description>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <java.version>25</java.version>
    <javafx.version>25.0.3</javafx.version>
    <test-plugins.version>3.3.0</test-plugins.version>
    <itext.version>9.6.0</itext.version>
    <testfx.version>4.0.18</testfx.version>
    <junit.version>6.1.0</junit.version>
    <logback.version>1.5.32</logback.version>
    <protobuf.version>4.35.0</protobuf.version>
    <protobuf.maven.plugin.version>0.6.1</protobuf.maven.plugin.version>
    <dependency-check-maven.version>12.1.3</dependency-check-maven.version>
    <!-- NVD API key: request at https://nvd.nist.gov/developers/request-an-api-key — pass -Dnvd.api.key=... or set env NVD_API_KEY (Jenkins) -->
    <nvd.api.key></nvd.api.key>
    <!-- 3.28+ bundles PMD 7 with Java 24 source parsing; 3.21.x/PMD 6 cannot parse Java 24 syntax -->
    <maven.pmd.version>3.28.0</maven.pmd.version>
    <spotbugs.maven.plugin.version>4.9.8.2</spotbugs.maven.plugin.version>
    <skipUTs>false</skipUTs>
    <skipITs>false</skipITs>
    <skipFTs>false</skipFTs>
    <surefire.groups></surefire.groups>
    <!-- Per Failsafe execution (integration vs functional); do not use one property for both. -->
    <failsafe.integrationGroups>integration</failsafe.integrationGroups>
    <failsafe.functionalGroups>functional</failsafe.functionalGroups>
  </properties>

  <!-- Align every transitive use of protobuf with protoc / generated code (${protobuf.version}). -->
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>${protobuf.version}</version>
      </dependency>
      <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java-util</artifactId>
        <version>${protobuf.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-base</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-controls</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-graphics</artifactId>
      <version>${javafx.version}</version>
    </dependency>
    <dependency>
      <groupId>org.openjfx</groupId>
      <artifactId>javafx-swing</artifactId>
      <version>${javafx.version}</version>
    </dependency>

    <dependency>
      <groupId>com.itextpdf</groupId>
      <artifactId>kernel</artifactId>
      <version>${itext.version}</version>
    </dependency>
    <!-- always needed -->
    <dependency>
      <groupId>com.itextpdf</groupId>
      <artifactId>io</artifactId>
      <version>${itext.version}</version>
    </dependency>
    <!-- always needed -->
    <dependency>
      <groupId>com.itextpdf</groupId>
      <artifactId>layout</artifactId>
      <version>${itext.version}</version>
    </dependency>
    <!-- only needed for PDF/A -->
    <dependency>
      <groupId>com.itextpdf</groupId>
      <artifactId>pdfa</artifactId>
      <version>${itext.version}</version>
    </dependency>

    <dependency>
      <groupId>jakarta.xml.bind</groupId>
      <artifactId>jakarta.xml.bind-api</artifactId>
      <version>4.0.5</version>
    </dependency>
    <dependency>
      <groupId>com.sun.xml.bind</groupId>
      <artifactId>jaxb-impl</artifactId>
      <version>4.0.8</version>
    </dependency>

    <dependency>
      <groupId>com.google.code.gson</groupId>
      <artifactId>gson</artifactId>
      <version>2.14.0</version>
    </dependency>

    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <version>${logback.version}</version>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-core</artifactId>
      <version>${logback.version}</version>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>2.0.18</version>
    </dependency>

    <dependency>
      <groupId>com.github.spotbugs</groupId>
      <artifactId>spotbugs-annotations</artifactId>
      <version>4.9.8</version>
      <scope>compile</scope>
    </dependency>

    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java</artifactId>
      <version>${protobuf.version}</version>
    </dependency>
    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java-util</artifactId>
      <version>${protobuf.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-engine</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testfx</groupId>
      <artifactId>testfx-core</artifactId>
      <version>${testfx.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testfx</groupId>
      <artifactId>testfx-junit5</artifactId>
      <version>${testfx.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.hamcrest</groupId>
      <artifactId>hamcrest-core</artifactId>
      <version>3.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>5.23.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>AdaLovesLace</finalName>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.15.0</version>
        <configuration>
          <source>25</source>
          <target>25</target>
        </configuration>
      </plugin>

      <plugin>
        <groupId>kr.motd.maven</groupId>
        <artifactId>os-maven-plugin</artifactId>
        <version>1.7.0</version>
        <executions>
          <execution>
            <goals>
              <goal>detect</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>0.0.8</version>
        <configuration>
            <mainClass>org.alienlabs.adaloveslace/org.alienlabs.adaloveslace.App</mainClass>
            <launcher>AdaLovesLace</launcher>
            <jlinkZipName>AdaLovesLace</jlinkZipName>
            <jlinkImageName>AdaLovesLace</jlinkImageName>
            <noManPages>true</noManPages>
            <stripDebug>true</stripDebug>
            <noHeaderFiles>true</noHeaderFiles>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>3.4.2</version>
        <configuration>
          <archive>
            <manifest>
              <mainClass>org.alienlabs.adaloveslace.NewMain</mainClass>
            </manifest>
          </archive>
        </configuration>
      </plugin>

      <!-- Call Conveyor -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.6.3</version>
        <executions>
          <execution>
            <id>conveyor-package</id>
            <phase>package</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>conveyor</executable>
              <arguments>
                <argument>make</argument>
                <argument>--output-dir=target/artifacts</argument>
              </arguments>
              <workingDirectory>${project.basedir}</workingDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <!-- JUnit 5 requires Surefire version 2.22.0 or higher -->
        <version>${test-plugins.version}</version>
        <configuration>
          <skipTests>${skipUTs}</skipTests>
          <groups>${surefire.groups}</groups>
          <!-- Merge JaCoCo agent with other JVM flags (same @{argLine} pattern as Failsafe). -->
          <argLine>@{argLine}</argLine>
          <includes>
            <include>**/unittest/**</include>
          </includes>
          <excludes>
            <exclude>**/functionaltest/**</exclude>
          </excludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>${test-plugins.version}</version>
        <executions>
          <!-- Conventional phase: integration tests run in integration-test; verification in verify. -->
          <execution>
            <id>integration-tests</id>
            <phase>integration-test</phase>
            <goals>
              <goal>integration-test</goal>
            </goals>
            <configuration>
              <skipITs>${skipITs}</skipITs>
              <groups>${failsafe.integrationGroups}</groups>
              <includes>
                <include>**/integrationtest/**</include>
              </includes>
              <excludes>
                <exclude>**/unittest/**</exclude>
                <exclude>**/functionaltest/**</exclude>
              </excludes>
            </configuration>
          </execution>
          <execution>
            <id>functional-tests</id>
            <phase>integration-test</phase>
            <goals>
              <goal>integration-test</goal>
            </goals>
            <configuration>
              <groups>${failsafe.functionalGroups}</groups>
              <includes>
                <include>**/functionaltest/**</include>
              </includes>
              <excludes>
                <exclude>**/unittest/**</exclude>
                <exclude>**/integrationtest/**</exclude>
              </excludes>
              <skipITs>${skipFTs}</skipITs>
              <!-- Reuse the forked JVM across TestFX classes: JavaFX/Xvfb startup is ~5s and dominates wall time when
                   not reused. AppFunctionalTestParent's @AfterEach closes toolbox/state/geometry stages and resets
                   static UI state (spinners, FastMoveModeButton, App.toolboxWindow/mainWindow) between tests. -->
              <reuseForks>true</reuseForks>
            </configuration>
          </execution>
          <execution>
            <id>failsafe-verify</id>
            <phase>verify</phase>
            <goals>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <forkCount>1</forkCount>
          <!-- TestFX/JavaFX: Application.launch can only be called once per JVM. Forks are reused across classes
               (see functional-tests execution); disable JUnit parallelism so TestFX tests don't race for the robot. -->
          <systemPropertyVariables>
            <junit.jupiter.execution.parallel.enabled>false</junit.jupiter.execution.parallel.enabled>
            <junit.jupiter.execution.parallel.mode.default>same_thread</junit.jupiter.execution.parallel.mode.default>
            <junit.jupiter.execution.parallel.mode.classes.default>same_thread</junit.jupiter.execution.parallel.mode.classes.default>
            <junit.jupiter.execution.parallel.mode.methods.default>same_thread</junit.jupiter.execution.parallel.mode.methods.default>
          </systemPropertyVariables>
          <useSystemClassLoader>true</useSystemClassLoader>
          <argLine>@{argLine} --enable-native-access=javafx.graphics --add-opens javafx.graphics/com.sun.javafx.application=org.testfx</argLine>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.owasp</groupId>
        <artifactId>dependency-check-maven</artifactId>
        <version>${dependency-check-maven.version}</version>
        <executions>
          <execution>
            <id>dependency-check-verify</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <!-- Empty = unauthenticated NVD (stricter rate limits). Invalid/garbage keys cause empty HTTP bodies and NPE in the client. -->
          <nvdApiKey>${nvd.api.key}</nvdApiKey>
          <nvdApiDelay>6000</nvdApiDelay>
          <failBuildOnCVSS>7</failBuildOnCVSS>
          <!-- OSS Index requires Sonatype credentials (401 without them). NVD/CISA/RetireJS etc. still run. -->
          <ossindexAnalyzerEnabled>false</ossindexAnalyzerEnabled>
        </configuration>
      </plugin>

      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.maven.plugin.version}</version>
        <configuration>
          <effort>Max</effort>
          <threshold>Low</threshold>
          <xmlOutput>true</xmlOutput>
          <failOnError>true</failOnError>
          <includeFilterFile>${basedir}/spotbugs-include.xml</includeFilterFile>
          <excludeFilterFile>${basedir}/spotbugs-exclude.xml</excludeFilterFile>
        </configuration>
        <executions>
          <execution>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>${maven.pmd.version}</version>
        <configuration>
          <rulesets>
            <ruleset>${basedir}/pmd-rules.xml</ruleset>
          </rulesets>
          <targetJdk>${java.version}</targetJdk>
          <failOnViolation>true</failOnViolation>
          <!-- Only priority 1 (highest); lower severities are warnings until backlog is cleared -->
          <failurePriority>1</failurePriority>
          <printFailingErrors>true</printFailingErrors>
          <skip>false</skip>
          <minimumTokens>90</minimumTokens>
          <excludeRoots>
            <excludeRoot>target/generated-sources</excludeRoot>
          </excludeRoots>
          <excludes>
            <exclude>**/generated/**</exclude>
          </excludes>
        </configuration>
        <executions>
          <execution>
            <id>pmd-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
          <execution>
            <id>cpd-check</id>
            <phase>verify</phase>
            <goals>
              <goal>cpd-check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.14</version>
        <configuration>
          <!-- Append across JVMs so separate mvn invocations (unit test, then IT/FT) accumulate in CI. -->
          <append>true</append>
          <destFile>${basedir}/target/coverage-reports/jacoco-unit.exec</destFile>
          <dataFile>${basedir}/target/coverage-reports/jacoco-unit.exec</dataFile>
          <!--
            Exclude auto-generated protobuf message classes. They live in src/main/java after protoc generation
            and contain thousands of lines of library-driven builder/parser code that is not ours to own and whose
            coverage is orthogonal to the quality of the adaloveslace code under test. Keeping them in the counter
            is also how the persistence.protobuf package fell to ~0.42: nothing realistic exercises every branch of
            a generated Builder. Excluding them here applies both to the coverage report and to the jacoco:check
            thresholds below, without masking any hand-written persistence code (LaceArchiveLoader/Saver/etc. live
            in the org.alienlabs.adaloveslace.persistence package, not persistence.protobuf).
          -->
          <excludes>
            <exclude>org/alienlabs/adaloveslace/persistence/protobuf/**</exclude>
            <!--
              NewMain is a two-line entrypoint that delegates to App.main, which blocks inside
              Application.launch for the life of the JVM. There is no deterministic way to execute its
              body from a surefire/failsafe fork without either forking another JVM per test or leaving
              a daemon thread parked inside the JavaFX event loop past JaCoCo's shutdown flush. The
              functional-test suite already starts the real application via TestFX, so the runtime path
              is thoroughly exercised; JaCoCo's coverage counter for this single delegation line is not
              a meaningful quality signal and is excluded to avoid blocking the per-class threshold on a
              race-prone test mechanism.
            -->
            <exclude>org/alienlabs/adaloveslace/NewMain.class</exclude>
          </excludes>
          <rules>
            <rule>
              <element>PACKAGE</element>
              <limits>
                <limit>
                  <counter>LINE</counter>
                  <value>COVEREDRATIO</value>
                  <minimum>0.60</minimum>
                </limit>
              </limits>
            </rule>
            <rule>
              <element>CLASS</element>
              <limits>
                <limit>
                  <counter>LINE</counter>
                  <value>COVEREDRATIO</value>
                  <minimum>0.15</minimum>
                </limit>
              </limits>
            </rule>
          </rules>
        </configuration>
        <executions>
          <execution>
            <id>jacoco-initialize</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>jacoco-site</id>
            <phase>package</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <execution>
            <id>jacoco-check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.xolstice.maven.plugins</groupId>
        <artifactId>protobuf-maven-plugin</artifactId>
        <version>${protobuf.maven.plugin.version}</version>
        <configuration>
          <protocArtifact>com.google.protobuf:protoc:${protobuf.version}:exe:${os.detected.classifier}</protocArtifact>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>compile</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>

    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-pmd-plugin</artifactId>
          <version>${maven.pmd.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

  <reporting>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jxr-plugin</artifactId>
        <version>3.4.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-pmd-plugin</artifactId>
        <version>${maven.pmd.version}</version>
        <configuration>
          <targetJdk>${java.version}</targetJdk>
          <rulesets>
            <ruleset>${basedir}/pmd-rules.xml</ruleset>
          </rulesets>
        </configuration>
      </plugin>
    </plugins>
  </reporting>

  <profiles>
    <profile>
      <id>unit-tests</id>
      <properties>
        <skipUTs>false</skipUTs>
        <skipITs>true</skipITs>
        <skipFTs>true</skipFTs>
        <surefire.groups>unit</surefire.groups>
      </properties>
    </profile>
    <profile>
      <id>integration-tests</id>
      <properties>
        <skipUTs>true</skipUTs>
        <skipITs>false</skipITs>
        <skipFTs>true</skipFTs>
      </properties>
    </profile>
    <profile>
      <id>functional-tests</id>
      <properties>
        <skipUTs>true</skipUTs>
        <skipITs>true</skipITs>
        <skipFTs>false</skipFTs>
      </properties>
    </profile>
    <!-- Single Maven invocation: all integration + functional ITs (use under Xvfb on CI). -->
    <profile>
      <id>ci-all-it</id>
      <properties>
        <skipUTs>true</skipUTs>
        <skipITs>false</skipITs>
        <skipFTs>false</skipFTs>
      </properties>
    </profile>

    <!-- CI pipeline markers: activated from Jenkins via -P (see src/main/resources/doc/ci-pipeline.md). -->
    <profile>
      <id>ci-pr</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <executions>
              <execution>
                <id>jacoco-check</id>
                <phase>none</phase>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-maven-plugin</artifactId>
            <!-- Warnings NG still receives XML from verify; backlog prevents hard fail on PR until remediated. -->
            <configuration>
              <failOnError>false</failOnError>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>ci-nightly</id>
    </profile>
    <profile>
      <id>ci-release</id>
    </profile>

    <profile>
      <id>visual-regression-tests</id>
      <properties>
        <!-- Only run the exec scaffold in test phase; do not re-run the full unit suite. -->
        <skipTests>true</skipTests>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>3.5.0</version>
            <executions>
              <execution>
                <id>visual-regression-scaffold</id>
                <phase>test</phase>
                <goals>
                  <goal>exec</goal>
                </goals>
                <configuration>
                  <executable>bash</executable>
                  <arguments>
                    <argument>${project.basedir}/scripts/ci/visual-regression.sh</argument>
                  </arguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>performance-tests</id>
      <properties>
        <skipTests>true</skipTests>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>3.5.0</version>
            <executions>
              <execution>
                <id>performance-sanity-scaffold</id>
                <phase>verify</phase>
                <goals>
                  <goal>exec</goal>
                </goals>
                <configuration>
                  <executable>bash</executable>
                  <arguments>
                    <argument>${project.basedir}/scripts/ci/performance-sanity.sh</argument>
                  </arguments>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
